<h1>This is the index page</h1>
<p>
    <% blogs.forEach(function(blog){ %>
        <div>
            <h2><%=blog.title%></h2>
            <img src="<%=blog.image%>" alt="" width="300px">
            <p><%=blog.created%></p>
            <p><%=blog.body%></p>
        </div>
    <% }); %>
</p>